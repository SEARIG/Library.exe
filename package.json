{
  "name": "ulc-universal-library-cloud",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only hosting,functions,firestore",
    "lint:functions": "cd functions && npm run lint"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0"
  }
}
